# scripts/student_test.sh output.
phase_*/output/

# The example's build artifacts, and the grading harness itself, which must
# never reach a student checkout.
phase_2/example/build/
phase_2/grader/

# LaTeX build byproducts; only the .tex and the .pdf are worth shipping.
*.aux
*.fdb_latexmk
*.fls
*.out
*.synctex.gz
**/documentation/*.log

__pycache__/
*.pyc
.claude
**/submission/*
!**/submission/.gitkeep

sim
*.vcd
